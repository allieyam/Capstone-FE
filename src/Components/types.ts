export interface Style {
  font: string;
  color: string;
}

export interface UserTypes {
  name: string;
  email: string;
  phone: string;
  keyskills: string;
  work: string;
  education: string;
}
